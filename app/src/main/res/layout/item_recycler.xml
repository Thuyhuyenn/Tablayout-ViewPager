<androidx.cardview.widget.CardView
    xmlns:android="https://www.google.com/search?q=http://schemas.android.com/apk/res/android"
    xmlns:app="https://www.google.com/search?q=http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="6dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <!-- ImageView: Sử dụng thư viện như Glide/Picasso để tải ảnh từ URL -->
        <ImageView
            android:id="@+id/item_image"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:scaleType="centerCrop"
            android:background="@color/design_default_color_primary_dark"
            android:src="@drawable/ic_launcher_background"
            android:contentDescription="Ảnh minh họa"/>

        <!-- TextView -->
        <TextView
            android:id="@+id/item_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Tiêu đề mẫu"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:gravity="center_horizontal"/>

    </LinearLayout>


</androidx.cardview.widget.CardView>